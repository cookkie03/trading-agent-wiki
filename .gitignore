# macOS / OS X system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Obsidian specific files (these change constantly and cause merge conflicts)
.trash/
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/
.obsidian/cookies
.obsidian/backups/
.obsidian/sync/


# Claude & Gemini caches / agent states
.claude/
.gemini/
storage_state.json

# Local Python cached files
.pytest_cache/
__pycache__/

# Obsidian Git conflict files
conflict-files-obsidian-git.md

# Graphify output (rigenerato, cambia di continuo → manda in loop Obsidian Git)
graphify-out/

# LightRAG KB (log e storage enormi e in continua scrittura + .env)
.lightrag/

# ============================================================
# SEGRETI E CREDENZIALI — non committare MAI nulla di questo.
# (Causa fuga dati: storage_state.json con cookie Google finito su repo pubblico, giu 2026)
# ============================================================

# --- File di ambiente / config con segreti ---
.env
.env.*
*.env
!.env.example
!.env.sample
!.env.template

# --- Stato sessione / automazione browser (Playwright, Puppeteer, Selenium) ---
storage_state.json
*storage_state*.json
*.storage_state.json
auth_state.json
*auth*state*.json
playwright/.auth/
.auth/
cookies.json
*cookies*.json
session.json
*session*.json
browser_state.json

# --- Credenziali / token / chiavi generiche ---
credentials.json
*credentials*.json
client_secret*.json
*client_secret*
token.json
*token*.json
*.token
tokens.json
service-account*.json
*service_account*.json
gcp-*.json
.netrc
.npmrc
.pypirc

# --- Chiavi crittografiche / certificati ---
*.pem
*.key
*.keyfile
*.p12
*.pfx
*.cer
*.crt
*.der
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.ppk

# --- Cloud / provider credentials ---
.aws/
aws-credentials*
*.aws.json
.azure/
.gcloud/
secrets.json
*secrets*.json
secrets.y*ml
*secret*.y*ml

# --- API key files comuni ---
apikey*
*apikey*
api_key*
*api_key*
*.apikey

# --- Altri stati agenti / cache locali ---
.gemini/
.codex/
.cursor/
storage_state*
*.session

# ============================================================
# FILE AUDIO / VIDEO — mai su GitHub (troppo pesanti, gonfiano il repo).
# Restano in locale / OneDrive, fuori da git. (rimossi giu 2026)
# ============================================================
*.mp3
*.m4a
*.wav
*.aac
*.flac
*.ogg
*.opus
*.wma
*.aiff
*.aif
*.amr
*.mp4
*.mov
*.m4v
*.avi
*.mkv
*.webm
*.wmv
*.flv